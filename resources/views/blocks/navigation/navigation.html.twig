<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <span class="navbar-brand">Events IIM</span>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

            <ul class="nav navbar-nav">
                {% if has_role(['moderator']) %}
                    <li><a href="{{ path_for('home') }}">Home</a></li>
                    <li><a href="{{ path_for('event.all') }}">Eventi</a></li>
                    <li><a href="{{ path_for('meetup.all') }}">Tavole Rotonde</a></li>
                    <li><a href="{{ path_for('accommodation.all') }}">Alloggi Partecipanti</a></li>
                {% endif %}
            </ul>

            <ul class="nav navbar-nav navbar-right">
                {% if auth.isLoggedIn %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            {% if auth.user.picture.filename %}
                                <img src="../../sites/default/files/styles/user_teaser__50_x_50_/public/pictures/{{ auth.user.picture.filename }}" width="30" height="30" class="img-circle" alt="User picture">
                            {% endif %}
                            Hello <b>{{ auth.user.field_first_name.und.0.value }}</b>
                            <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <!--<li><a href="{{ path_for('auth.password.change') }}">Change password</a></li>-->
                            <li><a href="{{ path_for('auth.signout') }}">Logout</a></li>
                        </ul>
                    </li>

                {% else %}
                    <!--<li>
                        <a href="{{ path_for('auth.signin') }}">Login</a>
                    </li>
                    <li>
                        <a href="{{ path_for('auth.signup') }}">Sign Up</a>
                    </li>-->
                {% endif %}
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
